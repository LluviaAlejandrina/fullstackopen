```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: User writes a note in the input field
    Note right of browser: User clicks "Save"

    Note right of browser: JavaScript prevents default form submission

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    Note right of browser: Content-Type: application/json
    Note right of browser: Payload: { content, date }
    activate server
    Note right of server: Server saves the new note
    server-->>browser: 201 Created (JSON response)
    deactivate server

    Note right of browser: JavaScript updates notes list in memory
    Note right of browser: JavaScript re-renders notes on the page
